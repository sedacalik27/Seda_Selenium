package day15_ExtentReport_Webtable_Excel;

public class Notlar {
    /*
    //exelden veri al

public String exeldenVeriAl(String dosyaYolu, String sayfa, int sutun , int satir ){
    FileInputStream fileInputStream = null;
    try {
        fileInputStream = new FileInputStream(dosyaYolu);
    } catch (FileNotFoundException e) {
        throw new RuntimeException(e);
    }
    Workbook workbook = null;
    try {
        workbook = WorkbookFactory.create(fileInputStream);
    } catch (IOException e) {
        throw new RuntimeException(e);
    }
    Sheet sheet = workbook.getSheet(sayfa);
    Row row = sheet.getRow(satir);
    Cell cell = row.getCell(sutun);
    return String.valueOf(cell);

}
     */
}
